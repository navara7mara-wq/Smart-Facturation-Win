[pytest]
testpaths = tests
addopts = -ra --strict-markers
filterwarnings = error
markers =
    unit: isolated business-rule tests without HTTP or persistent application state
    integration: database and service integration tests
    e2e: complete authenticated HTTP workflows
    migration: legacy database migration and rollback tests
